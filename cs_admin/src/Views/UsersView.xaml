<UserControl x:Class="TwitchSongAdmin.Views.UsersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal">
            <Button Content="Refresh" Command="{Binding RefreshCommand}"/>
            <TextBlock Text="{Binding Status}" Margin="10,0,0,0" Foreground="{DynamicResource BrushSubText}"/>
        </StackPanel>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Users}" AutoGenerateColumns="False" CanUserAddRows="False"
              HeadersVisibility="Column" IsReadOnly="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" IsReadOnly="True" Width="60"/>
                <DataGridTextColumn Header="Username" Binding="{Binding Username}" IsReadOnly="True" Width="*"/>
                <DataGridTextColumn Header="Twitch ID" Binding="{Binding TwitchId}" IsReadOnly="True" Width="200"/>
                <DataGridTextColumn Header="Points" Binding="{Binding PrioPoints, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Save Changes" Command="{Binding SaveCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
